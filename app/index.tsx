import { VStack } from "@/components/ui/vstack";
import { Text } from "@/components/ui/text";

export default function Index() {
  return (
    <VStack className="flex-1 justify-center items-center bg-red-500">
      <Text>This is the app</Text>
    </VStack>
  );
}
